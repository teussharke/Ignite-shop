import { styled } from "..";

export const HomeCointainer = styled('main', {
  display: 'flex',
  gap: '3rem',
  backgroundColor: 'blue',
  width: '100%',
  maxWidth: 'calc(100vw - ((100vw - 1180px) / 2))',
  marginLeft: 'auto',
  minHeight: 656,
})

export const Product = styled('a', {

})